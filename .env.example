# AI Provider Configuration
# Set to 'openai' or 'gemini' to choose which AI provider to use
AI_PROVIDER=openai

# Note: Depending on AI_PROVIDER value:
# - If AI_PROVIDER=openai, the application will load openai.conf
# - If AI_PROVIDER=gemini, the application will load gemini.conf
#
# Make sure to configure the appropriate .conf file with your API key
# before running the application.
